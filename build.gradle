apply plugin: 'java'
apply plugin: 'idea'

repositories {
    mavenCentral()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile 'xlloop:xlloop:0.3.+'
    testCompile("junit:junit:4.+")
}
